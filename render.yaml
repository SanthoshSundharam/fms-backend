services:
     - type: web
       name: laravel-app
       env: php
       buildCommand: |
         composer install
         php artisan config:cache
         php artisan route:cache
       startCommand: php artisan serve --host=0.0.0.0 --port=$PORT
       envVars:
         - key: APP_KEY
           generateValue: true
         - key: APP_ENV
           value: production
```

2. **Deploy:**
   - Go to [render.com](https://render.com)
   - Sign up with GitHub
   - Click "New +" → "Web Service"
   - Connect your Laravel repository
   - Render auto-detects the configuration
   - Add environment variables (TiDB connection details)
   - Deploy!

**Important:** The free tier sleeps after 15 minutes of inactivity (first request takes ~30 seconds to wake up)

---

## **Option B: Koyeb (Good Alternative)**

**Free Tier:** 1 web service, no sleep time!

**Steps:**
1. Go to [koyeb.com](https://koyeb.com)
2. Create account → "Create App"
3. Connect GitHub repository
4. Configure:
   - **Builder:** Dockerfile or Buildpack
   - **Port:** 8000
   - Add environment variables
5. Deploy

---

## **Option C: Cyclic.sh (Serverless)**

**Free Tier:** Unlimited apps, serverless

**Note:** Better for Node.js, but can work with PHP with custom setup

---

## **Option D: InfinityFree + 000webhost (Traditional Hosting)**

**For Laravel specifically:**

### **InfinityFree.net** (Recommended for PHP)
- Free PHP hosting with MySQL
- No sleeping/downtime
- Upload via FTP

**Steps:**
1. Sign up at [infinityfree.net](https://infinityfree.net)
2. Create account → Get FTP details
3. Upload Laravel files to `htdocs` folder
4. Create MySQL database in control panel
5. Update `.env` with their database credentials
6. Run migrations via PHP artisan in their file manager

---

## **My Recommended Setup (100% Free)**
```
Frontend: Vercel (No sleep, fast)
Backend: Render.com (Sleeps after 15 min)
Database: TiDB Cloud (Your existing setup)